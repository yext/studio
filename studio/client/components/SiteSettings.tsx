// TODO make this use a regular UI and not a json editor
export default function SiteSettings() {
  return null

  return (
    <div>
      <button
        className='btn'
        onClick={async () => {

        }}
      >
        Save
      </button>
    </div>
  )
}