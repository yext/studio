export const config = {
  stream: {
    $id: 'studio-stream-id_mock-uuid',
    filter: {},
    localization: { locales: ['en'], primary: false },
    fields: [],
  },
}

export default function () {
  return <></>
}
