import React from "react";
import { TemplateProps } from "@yext/pages";

export default function FunctionInlineExportLayout({
  document,
}: TemplateProps) {
  return <p>export default is inline with the function declaration</p>;
}
