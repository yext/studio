import React from "react";
import { TemplateProps } from "@yext/pages";

function EndOfFileExportLayout({ document }: TemplateProps) {
    return <p>export default is at the end of the file</p>
}

export default EndOfFileExportLayout;